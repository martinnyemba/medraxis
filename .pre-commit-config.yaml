# Mirrors the CI lint job (.github/workflows/ci.yml) so issues are caught
# locally before they reach a pull request.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix]
