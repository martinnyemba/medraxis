# Development & CI tooling (runtime deps are in requirements.txt).
-r requirements.txt

ruff>=0.6          # linting / import sorting (config in pyproject.toml)
coverage>=7.0      # optional: test coverage
